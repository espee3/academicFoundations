what happens when I create my schema in mysql instead of the codebase itself
will the schema still continue getting recreated everytime the server runs since it is in the code

what is the diff between db connection string and the actual connect - ref db.js
refer to prompt on perplexity app for more clarity and questions
doesnt db config already create the database - qtn to perplexity

is it possible to create something that keeps spawning up new tables depending on which word is inputed. more like ()********

code creates room for admin to create words and for users to input new word
maybe in future the code can verify if the words are true, however lets keep it simple for now

//next time I will confirm if so far so good my code is on track i.e the schema creation
//also check if it appears on my db, of course after researching through those questions
//using A.I then I will now work on the logic in my code notebook
